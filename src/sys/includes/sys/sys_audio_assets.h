/*
【模块职责】集中持有并注册所有启动预加载的 FATFS 音频PCM。
【重要约束】PCM加载后常驻到重启，确保应用打开和异步播放期间地址稳定；本模块不决定播放总线、音量或播放时机。
*/
#pragma once

namespace SysAudioAssets
{
    /**
     * 【接口说明】预加载并注册当前固件已知的全部FATFS音频。
     * 【返回值】全部成功返回true；单个资源失败不阻止其余资源继续加载。
     * 【重复调用】已经成功加载的资源会直接复用，不重复申请PSRAM。
     */
    bool PreloadAll();
}
